# Optional database connection. Leave unset to use local SQLite dev.db.
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/log_analyzer

# Optional local NLP model directory/name, if configured by the app.
# NLP_MODEL_PATH=
